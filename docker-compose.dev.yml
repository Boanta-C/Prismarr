# Prismarr — development overrides (hot reload)
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d

services:

  prismarr:
    environment:
      - APP_ENV=dev
      - APP_DEBUG=1
      - TZ=Europe/Paris
    volumes:
      - ./symfony:/var/www/html
