{% extends 'base.html.twig' %}

{% block title %}Infrastructure{% endblock %}
{% block page_title %}Infrastructure{% endblock %}

{% block body %}
  <div class="alert alert-info">
    <div class="d-flex align-items-center gap-3">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24"
           fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
           class="text-info flex-shrink-0">
        <circle cx="12" cy="12" r="10"/><path d="M12 16v-4m0-4h.01"/>
      </svg>
      <div>
        <div class="fw-semibold">Aucun device surveillé</div>
        <div class="text-secondary small">En attente du premier polling — vérifie que <code>argos_worker</code> est démarré.</div>
      </div>
    </div>
  </div>
{% endblock %}
