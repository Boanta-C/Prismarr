# Prismarr — configuration des services applicatifs.
# Le gros de l'autowiring fait le travail (attributs #[Autowire(env: ...)]
# sur les clients Media). Ce fichier reste volontairement minimal.

parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
