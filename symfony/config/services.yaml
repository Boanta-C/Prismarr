# Prismarr — application services configuration.
# Most of the work is done by autowiring (#[Autowire(env: ...)] attributes
# on the Media clients). This file is intentionally minimal.

parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/'
