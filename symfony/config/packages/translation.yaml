framework:
    default_locale: fr
    enabled_locales: ['fr', 'en']
    translator:
        default_path: '%kernel.project_dir%/translations'
        # fr is the source locale; en is the first translation. Other locales
        # added later should list their fallback here (e.g. ['es', 'fr']).
        fallbacks: ['fr']
